# Bounded Claim: Concept Contract-Extractor (Concept-CE)

**WP:** WP-ST-7 | **Project:** gem2-cbt
**Embedder:** sentence-transformers/all-MiniLM-L6-v2
**LLM backend:** deepseek-chat

## Methods evaluated

- **rule_lookup** — deterministic surface+context → sense table (ceiling)
- **embedding_nn** — frozen MiniLM-L6-v2 kNN over CLEAR-item exemplars
- **llm_prompt** — DeepSeek structured JSON extraction (temp=0)
- **majority** — fixed-default sense (floor)

## Main-domain results (50 items)

- **rule_lookup**: extraction_accuracy=1.000  confident_wrong=0.000  abstain_recall=1.000
- **embedding_nn** (⚠std=0): extraction_accuracy=0.100±0.000  confident_wrong=0.080  abstain_recall=0.600
- **llm_prompt** (⚠std=0): extraction_accuracy=1.000±0.000  confident_wrong=0.000  abstain_recall=1.000
- **majority**: extraction_accuracy=0.100  confident_wrong=0.000  abstain_recall=0.000

## Counter-probe results (U5B — memorization de-confound)

**Probe set:** 49 items — 24 novel-binding (made-up vocab defined in-prompt) + 25 adversarial-redefinition (real SI surfaces re-bound by an in-prompt contract).

- **rule_lookup_probe** (det): RAW acc=1.000; NORM acc=1.000; NORM cw=0.000; abstain_recall=1.000; memorization_revert_rate=0.000
- **llm_prompt_probe**: RAW acc=0.800; NORM acc=0.975; NORM cw=0.020; abstain_recall=1.000; memorization_revert_rate=0.050

*RAW vs NORM: the LLM frequently emits display-form sense names (e.g. "electric_inductance" for q_key "inductance"). The sense-string normalizer collapses these synonyms — NORM reflects extraction-correctness; RAW reflects raw string strictness.*

**Per-kind breakdown** (llm_prompt_probe, normalized):
- Novel-CLEAR: 20/20 (100%) — pure context-extraction works flawlessly when there is no SI prior to interfere
- Novel-AMBIG: 4/4 abstained — correct
- Adversarial-CLEAR: 19/20 (95%) after normalization — LLM followed the contract over the SI prior in 19 of 20 cases
- Adversarial-AMBIG: 5/5 abstained — correct
- Memorization revert: 1/20 = 5% (id=38: "pascal" with contract redefining to inductance → LLM returned "pressure")

## gate_ce verdict (strict: main AND probe both load-bearing for llm_prompt)

**gate_ce: PASS ✓**

- **embedding_nn**: FAIL
   - MAIN: acc 0.100 vs ≥0.900, cw 0.080 ≤ 0.1, ar 0.600 ≥ 0.6, beats floor: no
- **llm_prompt**: PASS
   - MAIN: acc 1.000 vs ≥0.900, cw 0.000 ≤ 0.1, ar 1.000 ≥ 0.6, beats floor: yes
   - PROBE: acc 0.975 vs ≥0.900, cw 0.020 ≤ 0.1, ar 1.000 ≥ 0.6, memorization_revert 0.050 ≤ 0.1

## Decision

**ADOPT Concept-CE method: llm_prompt** (with DeepSeek `deepseek-chat` backend at temp=0).  

Evidence for adoption:
- Main domain: llm_prompt matches the rule_lookup ceiling (1.000) on accuracy, perfect abstain, zero confident-wrong.
- Counter-probe: llm_prompt retains 0.975 (NORM) accuracy on novel + adversarial items, with only a 5% memorization-revert rate. This rules out the "LLM is just reciting memorized SI" confound — the LLM follows the in-prompt contract over its pretrained prior.
- Failure mode characterized: 1 of 20 adversarial-CLEAR items reverted to SI prior (id=38, surface="pascal"). Low but non-zero. Risk-aware deployment should flag adversarial cases.

**Next:**
- Plan Context-CE (WP-8) + Task-CE (WP-9) using the same llm_prompt / DeepSeek pattern.
- Route this CE's extracted contract into WP-6's oracle-payoff chain (close extraction → conditioning → violation-reduction).

**Caveats (load-bearing for any downstream adoption):**
1. Backend-specific: result is for `deepseek-chat`. A different LLM may show different extraction/memorization tradeoffs — re-run the probe per backend.
2. embedding_nn FAILED — the MiniLM-kNN approach is not viable on this domain (acc≈floor). LLM-CE is currently the ONLY viable non-lookup path.
3. Synthetic domain only. Real-world contract extraction (richer text, distractors, malformed contracts) is not tested here.

## Scope boundaries

- Bounded to this synthetic 10-quantity ECE micro-domain (50 main items + 49 probe items).
- Specific to embedder = `sentence-transformers/all-MiniLM-L6-v2` and LLM = `deepseek-chat` (temp=0).
- The probe shows in-context-extraction works; deployment in noisier real-world text is NOT tested.
- ECE contract generation and supervision-at-scale remain separate open problems.
- CBT-v1 remains GATED — unaffected by this experiment.
- Human/red-team sign-off required before any production adoption.

*Generated by WP-ST-7 | gem2-cbt*
