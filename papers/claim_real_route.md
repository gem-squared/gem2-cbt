# Bounded Claim: Real-NL Routing (CER cosmetic claim falsifier)

**WP:** WP-ST-8 | **Project:** gem2-cbt
**Encoder:** sentence-transformers/all-MiniLM-L6-v2
**Domain:** WSD sense-selection (real-NL routing analog)

## Per-router results (mean ± std across seeds)

- **softmax_raw**: recall@1=0.7036±0.0089  abstain_f1=0.8552±0.0062
- **softmax_sszes**: recall@1=0.3532±0.0096  abstain_f1=0.8575±0.0050
- **twofeature** (det): recall@1=0.3348±0.0000  abstain_f1=0.8522±0.0000
- **hpic_complex** (det): recall@1=0.3348±0.0000  abstain_f1=0.8556±0.0000
- **mfs** (det): recall@1=0.7013±0.0000  abstain_f1=nan±nan
- **keyword** (det): recall@1=0.2932±0.0000  abstain_f1=nan±nan
- **tfidf** (det): recall@1=0.2575±0.0000  abstain_f1=nan±nan

## Decomposition

- **hpic_complex vs softmax_raw** (recall@1): Δ=-0.3688, d=-41.40, wins 0/5 — *HEADLINE — complex vs strong baseline*
- **hpic_complex vs softmax_sszes** (recall@1): Δ=-0.0184, d=-1.91, wins 0/5 — *PROOF TEST — complex vs softmax over (ss,es)*
- **hpic_complex vs twofeature** (recall@1): Δ=+0.0000, d=nan, wins 0/1 — *complex-only delta (invertibility ⇒ Δ≈0)*
- **softmax_sszes vs softmax_raw** (recall@1): Δ=-0.3504, d=-31.34, wins 0/5 — *does the spread feature help?*
- **hpic_complex vs softmax_raw** (abstain_f1_oracle): Δ=+0.0004, d=0.06, wins 3/5 — *HEADLINE abstain — complex vs softmax*
- **hpic_complex vs softmax_sszes** (abstain_f1_oracle): Δ=-0.0020, d=-0.39, wins 2/5 — *PROOF TEST abstain*
- **hpic_complex vs twofeature** (abstain_f1_oracle): Δ=+0.0034, d=nan, wins 1/1 — *complex-only abstain delta*

## Decision

**gate_real_route FAIL — cosmetic CONFIRMED on real NL, *and* the spread/conflict feature does not survive even on its predicted home slice.**  

hpic_complex does NOT beat softmax_raw on recall@1 OR abstain-F1 beyond the pre-registered floor. The U5B probe verified this across every slice the spread feature might have survived on — ambiguous, low_margin, high_K, K_11+. The complex form and the (ss, es) aggregation BOTH collapse below softmax_raw on every cut.  

**Why this is more than "complex is cosmetic":**
- `softmax_raw` rec@1 ≈ `mfs` rec@1 (0.704 ≈ 0.701). The dominant signal in this real-NL routing task is the **most-frequent-sense prior** — softmax_raw learned to track it. Anything that throws away that prior throws away most of the routing skill.
- The (ss, es) aggregation **discards** the MFS prior: it collapses 5 raw per-candidate features (cosine, tfidf, MFS prior, lemma overlap, gloss-length) into 2 dimensions via ρ-weighted summation that does not preserve the rank-based MFS signal. softmax_sszes drops from 0.704 to 0.353 — half — because the dominant signal is gone.
- So the precise indictment of complex HPIC as a router is NOT "the spread feature is useless" — it is **"HPIC-style aggregation throws away the dominant task signal (MFS prior) on real NL routing."**

**Decision (router question CLOSED PERMANENTLY):**
- CER router = **softmax over raw per-candidate features** (finalized).
- The complex HPIC formalism is REJECTED for the routing/selection role across three independent fronts: algebra (invertibility proof), synthetic (WP-5 micro-domain), real NL (this WP, every slice).
- The (ss, es) aggregation is REJECTED as a routing primitive on this domain — it discards the MFS prior and halves recall.
- The spread feature **may still have abstain-only value on conflict-regime instances**, but the U5B per-slice probe shows even abstain_f1 has Δ≈0.001 vs softmax_raw on this domain — no detectable win.

**Team-play next move:** the cosmetic-as-router question is CLOSED. If David wants to keep HPIC anywhere, the only candidate left is a **post-hoc abstain layer** (not a router) on a DIFFERENT task where the dominant signal is NOT a rank-based prior — i.e., a task where (ss, es) is not throwing away the MFS dimension. WP-4's synthetic clear/insuff/conflict-regime task is exactly that shape; real NL routing is not.

## Scope boundaries

- Bounded to WSD as the real-NL routing analog (this specific WordNet sense set + glosses).
- Specific to encoder = `sentence-transformers/all-MiniLM-L6-v2`.
- A different encoder, a different domain, or learned ρ_k (not pre-registered) could change the picture.
- CBT-v1 remains GATED — unaffected by this experiment.
- Human/red-team sign-off required before any final architectural decision.

*Generated by WP-ST-8 | gem2-cbt*
